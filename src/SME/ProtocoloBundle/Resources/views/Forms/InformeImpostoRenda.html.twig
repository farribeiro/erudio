{% extends 'DGPBundle:Servidor:index.html.twig' %}

{% block headerTitle %}Requerimento de Informe para Imposto de Renda{% endblock %}

{% block page %}
    <form method="post" action="{{url('protocolo_servidor_incluirSolicitacao', {'solicitacao': solicitacao.id})}}">
        <div class="row">
            <div class="col-lg-12">
                <p>Este requerimento é inteiramente digital, dispensando entrega de documento impresso na Diretoria de Gestão de Pessoas.
                Clique em 'Confirmar' caso seus locais de trabalho estejam corretos:</p>
                <table  class="table table-striped table-hover">
                    <tbody>
                        <tr>
                            <th>Local</th>
                            <th>Função</th>
                            <th class="text-center">Carga Horária</th>
                        </tr>
                        {% for vinculo in vinculos %}
                            {% for alocacao in vinculo.alocacoes %}
                                <tr>
                                    <td>{{alocacao.localTrabalho.pessoaJuridica.nome}}</td>
                                    <td>{{alocacao.funcaoAtual}} </td>
                                    <td class="text-center">{{alocacao.cargaHoraria}} </td>
                              {% endfor %}
                        {% endfor %}
                    </tbody>
                </table>
                <p>* Para corrigir dados incorretos entre em contato com o gestor de sua unidade</p>
            </div>
        </div>
        <div class="row">
            <div class="col-lg-6">
                <button id="btnIncluir" type="submit" class="btn btn-primary">Confirmar</button>
            </div>
        </div>
    </form>
{% endblock %}